{
  "name": "opszero-typescript-aws-lambda-template",
  "version": "1.0.0",
  "description": "Default Template for Serverless functions on opsZero.",
  "main": "build/index.js",
  "repository": "https://github.com/abhiyerra/opszero-typescript-aws-lambda-template",
  "author": "opsZero",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^11.9.4",
    "axios": "^0.18.0"
  },
  "scripts": {
    "test": "mocha tests/unit/",
    "build": "tsc && cp package.json ./build/ && cd build && npm install --production"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.1"
  }
}